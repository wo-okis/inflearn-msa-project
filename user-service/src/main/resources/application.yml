server:
  port: 0

logging:
  level:
    com.wookis.userservice.client: DEBUG

spring:
  application:
    name: user-service
#  profiles:
#    active: dev
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
#  datasource:
#    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
#    username: sa
#    password:
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: user
      password: $2a$10$mQosdrabQmUwQ4nxsLnVKOwoxS7vFFBJr37twnIXvixAd/oMdVhgS
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include:
          - refresh
          - health
          - beans
          - info
          - busrefresh


greeting:
  message: Welcome to the Simple E-Commerce. (Spring Boot)

token:
  expired: 86400000 # 1 day in milliseconds
  secret: my-very-long-secret-key-1234567890123456